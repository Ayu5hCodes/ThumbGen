apiVersion: apps/v1
kind: Deployment
metadata:
  name: thumbgen-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: thumbgen-backend
          ports:
            - containerPort: 5000
